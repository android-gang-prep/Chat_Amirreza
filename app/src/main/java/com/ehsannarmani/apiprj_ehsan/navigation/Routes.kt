package com.ehsannarmani.apiprj_ehsan.navigation

enum class Routes(val route: String) {
    Splash(route = "splash"),
    AuthType(route = "auth-type"),
    SignUp(route = "sign-up"),
    SignIn(route = "sign-in"),
    Activation(route = "activation"),
    Home(route = "home"),
    Stream(route = "stream"),
    Chat(route = "chat")
}